# Regression corpus

World files (`*.thesis`) for failures that were fixed, replayed on demand to
prove they stay fixed. A world is content-addressed; do not edit one by hand.

Empty for now. The first entry is expected to be the pre-registered `stuckjob`
world once PRO-THESIS's confirmation gate can admit it (see
`../PREREGISTRATION.md`).
