# Dependencies and build output are recreated inside the image
node_modules
dist

# Secrets must never enter the build context
.env
.env.*
!.env.example

# Local tooling, scratch and binaries
.git
.tmp-ui-test
scratch
bin
*.log
*.tsbuildinfo

# Image definition itself
Dockerfile
.dockerignore
